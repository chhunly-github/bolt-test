<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>{{title}}</h1>
        <div class="users-list">
            {{#each users}}
                <div class="user-card">
                    <h2>{{this.name}}</h2>
                    <p>{{this.email}}</p>
                    <a href="/users/{{this.id}}" class="view-button">View Details</a>
                </div>
            {{/each}}
        </div>
    </div>
</body>
</html>